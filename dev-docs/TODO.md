Wishlist
- Global variables (no magic number and names)
- debug and protections
- what is sh and why does that prevent need to give admin permissions 
- claude projects should have a git initialised with its first commit set up, an empty todo, and an empty spec.
- Make the jlink flasher available with config files
- Can JLink detect and confirm that the target device matches the given MCU....
- list scripts breaks on change of directory. This needs to be amended
- Easy navigation of the file system when creating repo mix